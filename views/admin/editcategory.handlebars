<h4>Edit categoooory</h4>

<form action="/admin/category/edited" method="POST">
    <label for="nome">Name: </label>
    <input type="hidden" name="id" value="{{category._id}}"></input>
    <input type="text" id="name" name="name" placeholder="Name category" class="form-control" value="{{category.name}}">
    <label for="slug">Slug: </label>
    <input type="text" name="slug" placeholder="Slug" id="slug" class="form-control" value="{{category.slug}}">
    <br>
    <button class="btn btn-success">Edit category</button>

</form>